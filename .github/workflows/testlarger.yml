name: Test Larger Runner

on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: testiin
    steps:
      - name: Confirm runner is reachable
        run: |
          echo "Runner name: $RUNNER_NAME"
          echo "Runner OS: $RUNNER_OS"
          uname -a
